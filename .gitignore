# Secrets
.env
esi_token.json

# Python
__pycache__/
.venv/
*.pyc

# Databases (local cache / SDE — too large for git)
crest_history.db
market_cache.db
sqlite-latest.sqlite

# crest.db is small enough to commit, but exclude if preferred:
# crest.db

# Node
node_modules/
dist/